<% if @gear.errors.any? %>
<div id="error_explanation">
 <h2><%= pluralize(@gear.errors.count, "error") %> prohibited this gear from being saved:</h2>
 <ul>
 <% @gear.errors.full_messages.each do |message| %>
  <li><%= message %></li> <% end %>
  </ul>
  </div>
  <% end %>
  
<h1>New Gear</h1>
<%= simple_form_for @gear do |f| %>
  <%= f.input :title %>
   <%= f.input :description %>
   <%= f.input :price %>
  <%= f.button :submit %>
<% end %>
<%= link_to 'Back to gears', gears_path, class: 'btn btn-secondary' %>
